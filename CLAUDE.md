# Claude Code Instructions

Follow `AGENTS.md` for this repository.

Flow's canonical agent behavior lives in `packages/flow/assets/agent-os/`. Claude-specific runtime files must be generated or projected from Agent OS, not maintained as separate prompt sources.
