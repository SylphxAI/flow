# Claude Code Instructions

Follow `AGENTS.md` for this repository.

Flow's canonical agent behavior lives in `packages/flow/assets/`. Claude-specific runtime files must be generated or projected from those canonical assets, not maintained as separate prompt sources.
