version = "1"

[project]
name = "@sylphx/flow-monorepo"
slug = "flow"
org = "sylphx"

[build]
strategy = "auto"
context = "docs"

[[services]]
name = "web"
type = "web"
port = 80
watch_paths = ["docs/**"]
