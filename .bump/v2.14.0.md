# v2.14.0 - Review & Act Mode

## @sylphx/flow

### Features

- **commands**: Change all 25 review commands from "report mode" to "action mode"
  - Old: Report findings and recommendations
  - New: **Review then Act** — identify issues, then implement fixes directly
  - Philosophy: Don't just report — fix

- **claude-code**: Add default settings for optimal experience
  - `CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000` for larger context output
  - Empty `attribution.commit` and `attribution.pr` for cleaner output
  - `alwaysThinkingEnabled: true` for extended thinking mode
