# v2.14.0 - Claude Code Default Settings

## @sylphx/flow

### Features

- **claude-code**: Add default settings for optimal experience
  - `CLAUDE_CODE_MAX_OUTPUT_TOKENS=128000` for larger context output
  - Empty `attribution.commit` and `attribution.pr` for cleaner output
  - `alwaysThinkingEnabled: true` for extended thinking mode
  - These defaults apply when running `flow init` for Claude Code targets
